# Flat node_modules (no symlinks) so Vercel can package serverless functions
# that include the @terminal3 WASM SDK. pnpm's default symlinked layout makes
# Vercel reject the function bundle ("files in symlinked directories").
node-linker=hoisted
